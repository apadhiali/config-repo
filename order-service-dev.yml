global:
  message: Welcome to order service in dev environment!
server:
  port: 8082
message: Hello from Dev environment!
spring:
  datasource:
    url: jdbc:h2:mem:order_master
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
