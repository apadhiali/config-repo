global:
  message: Welcome to customer service in prod environment!
server:
  port: 8083
message: Hello from Prod environment!
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cust_prod
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
